import React, { Component, } from 'react';

class NavBar extends Component {
    state = {  }
    render() { 
        return (
            <nav className="navbar bg-dark">
                <h1 className="text-white">Working with an Api</h1>
            </nav>
        );
    }
}
 
export default NavBar;